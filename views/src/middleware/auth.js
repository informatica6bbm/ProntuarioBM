export default function auth({ to, from, next, router }) {
    // if (!localStorage.getItem('jwt')) {
    //     return router.push({ name: 'login' });
    // }

    // localStorage.setItem('nome',to);
    // consolelog("tetse");
    // return next({
    //     path: '/',
    //     query: {
    //      redirect: to.fullPath,
    //    },
    // });
}
